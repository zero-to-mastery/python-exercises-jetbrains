# Get the length of the word
word = "Python"
word_length = len(word)

# Create a line of 10 dashes
separator = "-" * 10

# Mask a password by replacing each character with an asterisk
password = "secret123"
masked_password = "*" * len(password)

# Create a border line (=) with the same length as a message
message = "Welcome"
border = "=" * len(message)

# Count characters in a username
username = "alex_2024"
username_length = len(username)
